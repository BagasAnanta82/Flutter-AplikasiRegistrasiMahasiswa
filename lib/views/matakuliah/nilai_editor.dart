import 'package:flutter/material.dart';

class NilaiEditor extends StatelessWidget {
const NilaiEditor({Key? key}) : super(key: key);

@override
Widget build(BuildContext context) {
return Scaffold(
appBar: AppBar(title: const Text('Ubah Nilai')),
body: const Center(child: Text('Konten belum dibuat')),
);
}
}
